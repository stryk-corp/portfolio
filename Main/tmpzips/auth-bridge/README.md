Authentication Bridge

Utilities and examples to bridge desktop apps and web authentication flows (OAuth, token exchange).

Contents:
- `auth_bridge_example.js` - example Node module showing a token exchange mock and local storage example.

Notes:
- This is a demonstration; replace mocks with real OAuth flows in production.