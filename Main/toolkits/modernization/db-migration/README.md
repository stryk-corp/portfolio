DB Migration Toolkit

This toolkit includes scripts to transform and validate data when migrating from legacy databases.

Contents:
- `migrate_sample.py` - example Python script that reads a CSV export and outputs normalized JSON suitable for import.

Notes:
- The example is self-contained and does not require a live database. Replace CSV input with actual exports when running.