# .env.example - removed

This file previously contained placeholder environment variables for a Node/Express prototype.
To avoid leaving credential templates or secrets in the repository the file has been removed.

If you need to run a local server, create a private .env file outside of source control with the
required variables. Do not commit secrets to the repository.
